name: task
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
#  optimist_dev_common:
#    git: git@gitlab.com:optimist.dev/common.git
  built_value: ^7.1.0
  rxdart: ^0.24.0
  cached_network_image: ^2.1.0+1
  cupertino_icons: ^0.1.3
  provider: ^4.1.0-dev
  audioplayers: ^0.15.1
  flutter_svg: ^0.17.4
  shared_preferences: ^0.5.7
  intl_translation: ^0.17.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.9.0
  built_value_generator: ^7.1.0
  flutter_localizations:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
