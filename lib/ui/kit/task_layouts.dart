import 'package:flutter/cupertino.dart';

class TaskLayouts {
  static const padding = 16.0;
  static const horizontalPadding = EdgeInsets.symmetric(horizontal: padding);
  static const borderRadius = 6.0;
  static final circularBorderRadius = BorderRadius.circular(borderRadius);
  static final roundedBorder =
      RoundedRectangleBorder(borderRadius: circularBorderRadius);
}
